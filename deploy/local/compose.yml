# Local-dev base for tileserver-rs.
#
# Stand alone:        docker compose -f deploy/local/compose.yml up
# With postgres-dev:  docker compose -f deploy/local/compose.yml -f deploy/local/compose.override.yml up -d
#
# For production deployment, see deploy/prod/.

services:
  tileserver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tileserver
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PORT:-8080}:8080"
    volumes:
      - ./data:/data:ro
      - ./config.toml:/app/config.toml:ro
    environment:
      - RUST_LOG=${RUST_LOG:-debug}
      - HOST=0.0.0.0
      - PORT=8080
      - TILESERVER_PUBLIC_URL=${TILESERVER_PUBLIC_URL:-}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
